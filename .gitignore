# Environment variables (contains API keys)
.env
someo-park-investment-management/.env

# Claude Code
.claude/

# VS Code
.vscode/

# Onboarding guide (contains env-specific paths, not for version control)
ONBOARDING.md

# macOS
.DS_Store

# Chronos Module
chronos-forecasting-main/
chronos-2-finetuned/

# Qlib — ignore everything except sector_rotation, semiconductor_strategy, and env export files
qlib-main/*
!qlib-main/sector_rotation
!qlib-main/semiconductor_strategy
!qlib-main/qlib_run_conda_export.txt
!qlib-main/qlib_run_env.yml
!qlib-main/qlib_run_requirements.txt

# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# Data / output (large files, not for version control)
price_data/
sector_rotation/    # stale cache from old loader runs; ETF data now lives in price_data/sector_etfs/
charts/
logs/

# Archive of old runs (never commit)
archive/

# historical_runs: ignore everything except specific walk-forward output files
# Note: must NOT ignore the directory itself — use wildcard on contents instead
historical_runs/*
!historical_runs/wf_diagnostic_*.xlsx
!historical_runs/walk_forward/
!historical_runs/walk_forward_mtfs/
historical_runs/walk_forward/*
!historical_runs/walk_forward/walk_forward_summary_*.json
!historical_runs/walk_forward/oos_pair_summary_*.csv
!historical_runs/walk_forward/oos_equity_curve_*.csv
!historical_runs/walk_forward/dsr_selection_log_*.csv
historical_runs/walk_forward_mtfs/*
!historical_runs/walk_forward_mtfs/walk_forward_summary_*.json
!historical_runs/walk_forward_mtfs/oos_pair_summary_*.csv
!historical_runs/walk_forward_mtfs/oos_equity_curve_*.csv
!historical_runs/walk_forward_mtfs/dsr_selection_log_*.csv

# Daily trading signals & reports — all files tracked
.env.example

# Pipeline state
pipeline_state/

# Factsheet generator
factsheet_generator/

# Backup files
*_backup.json
*.bak

# MLflow tracking database (runtime, not for version control)
mlruns/

# Sector rotation: generated outputs
qlib-main/sector_rotation/report/output/
qlib-main/sector_rotation/logs/
qlib-main/sector_rotation/backtest_results/
qlib-main/sector_rotation/inventory_history/
qlib-main/sector_rotation/trading_signals/
qlib-main/sector_rotation/pipeline_state/
qlib-main/sector_rotation/notebooks/

# Semiconductor strategy: generated outputs (mirror sector_rotation exclusions)
qlib-main/semiconductor_strategy/report/output/
qlib-main/semiconductor_strategy/logs/
qlib-main/semiconductor_strategy/backtest_results/
qlib-main/semiconductor_strategy/inventory_history/
qlib-main/semiconductor_strategy/trading_signals/
qlib-main/semiconductor_strategy/pipeline_state/
qlib-main/semiconductor_strategy/notebooks/

# Private Credit module — now tracked in someopark-test (de-nested 2026-06). Its CODE is
# committed here; generated data + secrets are excluded by its own
# portfolio_of_private_credit_deals/.gitignore (config.py, .env, fred_rates.csv,
# bdc_results/, cashflows_*/, *.key, *.pem, …).
public/

# Zip archives
*.zip

# Private keys — never commit any key material anywhere in the repo
*.key
*.pem

# prediction_market — World Cup trading project (isolated): secrets + generated data
# (its own prediction_market/.gitignore also covers these; this is belt-and-suspenders)
prediction_market/.env
prediction_market/**/*.key
prediction_market/**/*.pem
prediction_market/data/output/
prediction_market/data/raw/
prediction_market/data/logs/
prediction_market/data/wc.db
prediction_market/data/wc.db-wal
prediction_market/data/wc.db-shm
prediction_market/data/kalshi_docs/

# Prediction-market pricing research (papers, figures, tick data, scratch) — keep out of git
prediction_market/research/
