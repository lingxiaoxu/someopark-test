# Secrets — never commit
.env

# Market data + generated outputs (large, regenerable/recorded)
price_data/
trading_signals/
logs/

__pycache__/
*.pyc
.pytest_cache/

# W8's reproducible replay is maintained alongside its live kernel and tests.
!crypto_strategies/event_binary/research_complete_set.py
