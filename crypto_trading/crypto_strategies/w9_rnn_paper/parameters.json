{
  "policy": "flow_budget50_daily_tuned_rnn_risk_half_v1",
  "mode": "prospective_conditional_parent_paper_only",
  "research_variant": "flow_budget50_tuned_rnn_risk_half",
  "research_model": "recent_refit_model",
  "research_report": "trading_signals/reports/w9_rnn_pit_expanded_20260916T001013Z",
  "parent": "W7 registered MAIN paper entries [.78,.98], 25 contracts",
  "base_contracts": 25,
  "risk_contract_cap": 12,
  "common_expiry_side_cost_budget_usd": 50,
  "flow_skip": "valid prior-minute held-direction return < 0 AND held-direction observed flow imbalance <= -0.5",
  "risk_trigger": "predicted absolute return strictly greater than this asset's prior-7-day q90",
  "prediction_max_age_seconds": 60,
  "source_discovery_max_delay_seconds": 90,
  "missing_features": "Keep the signal and other available controls; unknown flow does not skip, unknown model does not halve. Coverage recorded separately.",
  "training": "Daily UTC: select config and epoch on prior5days; fresh3seed refit latest28days. Prior7day stored out-of-fold volume forecasts calibrate risk. CPU1/nice10.",
  "model_activation": "Only after training finishes, immutable artifacts are complete and atomic publication records actual availability time.",
  "live_forecast": "Persisted prediction, model publication and all input receipts must precede the parent decision. No retrospective predictions in the live ledger.",
  "fees": "Inherit W7 rounded paper fees and proportional settled parent PNL; no exchange fill claim.",
  "legacy_control": "Existing w9_w10_paper remains unchanged and is a separate older flow-only control.",
  "first_review_classified_expiry_windows": 300,
  "historical_results_not_seeded": true,
  "network_enabled": false,
  "demo_orders_enabled": false,
  "prod_orders_enabled": false,
  "frontend_export_enabled": false
}
